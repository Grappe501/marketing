# Set on Netlify (Site settings → Environment variables) for correct Open Graph URLs.
# Example: https://your-subdomain.netlify.app
NEXT_PUBLIC_SITE_URL=

# Optional: set to "true" on preview deploys to emit noindex and disallow crawling.
NEXT_PUBLIC_NO_INDEX=
